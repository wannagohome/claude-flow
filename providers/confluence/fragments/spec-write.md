## Confluence PRD Reference

{PRD_URL}

Use MCP tools (`atlassian`) to read Confluence documents:
- Extract functional requirements, business rules, acceptance criteria
- Explore all child pages if they exist
- Tag ambiguous or contradictory sections with [undefined] or [contradiction]

If no PRD_URL is configured, check if the user provided a Confluence URL
in requirements-input.md or other input documents.
